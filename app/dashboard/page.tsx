export default function DashboardPage() {
  return (
    <main className="min-h-screen bg-[#090909] flex items-center justify-center px-4">
      <div className="text-center space-y-3">
        <h1 className="text-2xl font-bold text-white">LegalMind Dashboard</h1>
        <p className="text-sm text-gray-400">Workspace placeholder — auth flow complete.</p>
        <a href="/" className="inline-block text-sm text-blue-400 hover:text-blue-300">
          ← Home
        </a>
      </div>
    </main>
  );
}
